.venv/
__pycache__/
.env
.DS_Store

# Dataset (downloaded, not source) — re-fetch locally
data/*
# ...except the 500-image subset the committed index points at, which ships with the repo
!data/subset/

# React build output (generated by `npm run build` in frontend/)
frontend/dist/
frontend/node_modules/
